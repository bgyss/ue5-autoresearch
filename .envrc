use flake

# Install pinned tool versions (python, uv) via mise.
mise install >/dev/null

# Sync uv-managed python dependencies.
mise exec -- uv sync --quiet

# Activate the uv-managed venv.
source .venv/bin/activate
