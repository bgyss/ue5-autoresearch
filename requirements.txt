# Python dependencies for the harness scripts.
# The benchmark itself requires a local Unreal Engine 5.4+ editor or packaged build.

# Evaluator / quality metric
numpy>=1.24.0
scipy>=1.10.0
Pillow>=9.0.0

# Ollama uses its OpenAI-compatible endpoint; the proposer uses only stdlib.
# If you prefer the official client, uncomment:
# openai>=1.0.0
